__pycache__/
*.py[cod]
.DS_Store
.pytest_cache/
.venv/

# Packaged skills, built from the source folders and not committed.
*.zip
*.skill
dist/

# The setup lock. It appears if you run the boilerplate from a clone rather
# than from an installed copy; it records local state, not repository state.
.configured
