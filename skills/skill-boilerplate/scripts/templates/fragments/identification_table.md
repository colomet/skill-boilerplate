### Identification table

Right after the title, a table with these fields:

{{FIELDS}}

Keep it to facts that don't rot. A field nobody maintains is worse than no
field: it reads as current and isn't.
