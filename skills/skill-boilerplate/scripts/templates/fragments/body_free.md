### Body structure

No fixed template. Write what the skill needs.

Two things are worth including anyway, whatever shape you choose:

- **When NOT to use this.** Overlap between skills is the most expensive
  failure in a growing collection, and this is the section that prevents it.
- **What the output looks like.** A skill that doesn't say what "done" looks
  like produces something different every run.
