## Versioning

Not used. Skills carry no version number.

Nothing in the skill format requires one — `name` and `description` are the only
mandatory frontmatter fields. If you later want versions, SemVer
(`MAJOR.MINOR.PATCH`) is the software default, and it goes under `metadata` in
the frontmatter, never at the top level.
